# CHANGELOG

## v1.6.1
- 不怎么看github，感兴趣的可以加加反馈或聊天画画QQ群：945824082 
- 新增图库功能，让氛围转移，角色参考，图生图更加便捷
- 新增预设图库ck个人数据功能，配置可选择是否公开所有用户的预设图库或ck
- 新增预设图库ck预览图功能，若无预览图则提取第一次生成的图片。
- 支持收集被引用消息里的图片，已接入手动绘图、自动绘图和 LLM 工具入口。
- Vibe Transfer 会先调用缓存以复用图片避免连续扣费。
- 缓存键包含模型、图片内容和 information_extracted，避免跨模型误用缓存。（人话就是生成的图片和缓存的图片分开存，以免有时候被QQ拦截找不到图片）
- 编码失败直接抛出完整错误，不再把原始图片错误地当成编码结果。
- 让 LLM 绘图调用正确传递共享 HTTP client 。
- 新增缓存管理器
- 新增nai5模型选择
- image.novelai.net无法直连，非国外机器需要proxy，新增自定义代理网址
- 其中三个文件夹的位置分别是AstrBot/data/plugin_data/astrbot_plugin_ppnai/presets.json   # 预设
├── cs/
│   └── <用户ID>/
│       └── <CK名称>.txt          # CK/角色保持
├── image_library/
│   ├── index.json                # 图库名称、文件和 MIME 信息
│   └── images/
│       └── <sha256>.bin          # 图库原图
└── previews/
    ├── index.json                # 资源与预览图的对应关系
    └── images/
        ├── preset_*.png          # 预设预览图
        ├── ck_*.png              # CK 预览图
        └── image_*.png           # 图库预览图

## v1.5.7

Enhancements:
- 新增支持nai画图和自动画图上传图片功能 
- 面板新增默认预设，现在是若没有正向提示词，先读取是否预存了预设，若没有，则读取默认正向，负向同理，若有提示词则覆盖。
- 为了方便编写，我将预设1,预设2,预设3...改成s1,s2,s3...;描述改为ds
- 新增批量生成功能
- 批量生成功能扩展到 nai画图 与 自动画图（预设里可写 n=）
- 批量生图时每张图都会重新走 LLM 生成参数（nai画图/自动画图）
- 在配置项添加了一个新的开关，是否将结果合并为聊天记录
- 新增角色保持（cs）存档与引用，可用于 nai、nai画图、nai自动画图
- 新增角色保持指令：cs dcs scs ccs，并支持参数 cs=名称
- 新增n额度上限限制，csaa扣除额度
- 修改了提示词以提高成功率
- 将s映射为s1，cs映射为cs1，并可使用多个cs组合

## 紧急更新
- 解决了所有人都能使用管理员指令的bug